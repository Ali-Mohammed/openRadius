# =============================================================================
# OpenRadius Unattended Installation Config
# =============================================================================
# Copy this file and customize it for your environment:
#   cp scripts/install-config.example.env /root/install-config.env
#   nano /root/install-config.env
#
# Then run:
#   sudo ./install-openradius.sh --unattended --config /root/install-config.env
# =============================================================================

# ── Required ──────────────────────────────────────────────────────────────────
DOMAIN=example.com
SSL_EMAIL=admin@example.com

# ── Password Mode ─────────────────────────────────────────────────────────────
# "auto"   → All passwords auto-generated (recommended)
# "custom" → Provide POSTGRES_PASSWORD, KEYCLOAK_ADMIN_PASSWORD, REDIS_PASSWORD
PASSWORD_MODE=auto

# Only needed if PASSWORD_MODE=custom (minimum 16 characters each):
# POSTGRES_PASSWORD=your_strong_password_here
# KEYCLOAK_ADMIN_PASSWORD=your_strong_password_here
# REDIS_PASSWORD=your_strong_password_here

# ── Optional Settings ─────────────────────────────────────────────────────────
# Install sample/demo data? (y/n)
INSTALL_SAMPLE=n

# Auto-configure Keycloak realm, clients, roles, and service accounts? (y/n)
CONFIGURE_KEYCLOAK=y

# Enable automated daily backups? (y/n)
ENABLE_BACKUP=y

# Skip Let's Encrypt SSL and use self-signed certificates? (true/false)
# Use true if DNS is not yet configured; you can generate real certs later.
SKIP_SSL=false
