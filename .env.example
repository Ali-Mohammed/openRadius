# =============================================================================
# OpenRadius - Production Environment Configuration
# =============================================================================
# Copy this file to .env and update with your production values
# Usage: cp .env.example .env
# =============================================================================

# -----------------------------------------------------------------------------
# Database Configuration
# -----------------------------------------------------------------------------
POSTGRES_PASSWORD=CHANGE_THIS_SECURE_PASSWORD

# -----------------------------------------------------------------------------
# Keycloak Configuration
# -----------------------------------------------------------------------------
KEYCLOAK_ADMIN_PASSWORD=CHANGE_THIS_SECURE_PASSWORD

# -----------------------------------------------------------------------------
# Redis Configuration
# -----------------------------------------------------------------------------
REDIS_PASSWORD=CHANGE_THIS_SECURE_PASSWORD

# -----------------------------------------------------------------------------
# OIDC/Keycloak Configuration
# -----------------------------------------------------------------------------
# Default values work for internal Docker network
# Change these if accessing Keycloak from external domains
OIDC_AUTHORITY=http://keycloak:8080/realms/openradius
OIDC_METADATA_ADDRESS=http://keycloak:8080/realms/openradius/.well-known/openid-configuration
OIDC_ISSUER=http://keycloak:8080/realms/openradius

# For external access, use your domain:
# OIDC_AUTHORITY=https://auth.yourdomain.com/realms/openradius
# OIDC_METADATA_ADDRESS=https://auth.yourdomain.com/realms/openradius/.well-known/openid-configuration
# OIDC_ISSUER=https://auth.yourdomain.com/realms/openradius

# -----------------------------------------------------------------------------
# Switch Decryption
# -----------------------------------------------------------------------------
# Replace with your actual switch decryption key (hexadecimal format)
SWITCH_DECRYPTION_KEY=YOUR_SWITCH_DECRYPTION_KEY_HEX

# -----------------------------------------------------------------------------
# Seq Logging (Optional)
# -----------------------------------------------------------------------------
# Leave empty if you don't need API key authentication for Seq
SEQ_API_KEY=

# -----------------------------------------------------------------------------
# Docker Configuration
# -----------------------------------------------------------------------------
# Uncomment and set if you want to use specific versions instead of :latest
# BACKEND_VERSION=v1.0.0
# FRONTEND_VERSION=v1.0.0

# -----------------------------------------------------------------------------
# Additional Backend Configuration (Optional)
# -----------------------------------------------------------------------------
# Uncomment and modify if needed
# ASPNETCORE_ENVIRONMENT=Production
# ASPNETCORE_HTTPS_PORT=5001

# -----------------------------------------------------------------------------
# Network Configuration (Optional)
# -----------------------------------------------------------------------------
# Uncomment to expose services on different ports
# POSTGRES_PORT=5432
# KEYCLOAK_PORT=8080
# REDIS_PORT=6379
# BACKEND_PORT=5000
# FRONTEND_PORT=80
# SEQ_PORT=5341
# REDPANDA_CONSOLE_PORT=8090
# DEBEZIUM_PORT=8083
